# scripts/

Helper scripts for running experiments on remote compute.

Planned (post day-1):
- `launch_lambda.sh` — provision a Lambda GPU instance via API
- `run_remote.sh` — sync repo to the box, run a training config, pull results back

For now this folder is a placeholder so the layout is committed.
